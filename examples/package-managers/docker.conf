# docker.conf — container images via docker pull
#
# Pulls Docker images using the system docker command.
# The DockerPackageManager maps "install" to "docker pull".

package {
  type = "package"
  package_manager = "docker"
  operation = "install"
  packages += "hello-world"
  packages += "alpine"
  skip_if_installed = true
}
